# AmazonClone — Step 4 React Shopping App

An Amazon.com-inspired shopping app built with React, Bootstrap 5, and JSON Server.

---

## 🚀 How to Run

### 1. Start the Backend (JSON Server)

```bash
cd todo-backend
npm install
npm start
```

> The backend runs on **http://localhost:3001**
> If `npm start` doesn't work with your json-server version, try:
> `npx json-server db.json --port 3001`

### 2. Start the Frontend (React)

Open a **second terminal**:

```bash
cd todo-frontend
npm install
npm start
```

> The app opens at **http://localhost:3000**

---

5/6 Updates
- Added Proceed to Checkout Page
- Added 2 more item listings
- Added A Customer Service help section as well as a gift cards section
- Added a sign-in section
